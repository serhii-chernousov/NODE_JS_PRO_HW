# Скопіюй у .env (не коміть). Потрібні лише для runtime compose, не для Dockerfile.
# POSTGRES_PASSWORD у compose без дефолта — без цього файлу стек не підніметься.
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=app
